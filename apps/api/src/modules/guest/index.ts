/**
 * Module guest · interface publique (Lot 8 — guest checkout).
 *
 * Référence : ARCHITECTURE.md §3 (« Communication entre modules uniquement
 * via leur interface publique »), CLAUDE.md §G3 (mode invité séparé).
 */

export { guestPlugin } from './guest-plugin.js';
export { guestService } from './guest-service.js';
